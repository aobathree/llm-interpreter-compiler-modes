# LLM活用における「インタープリター・モード」と「コンパイル・モード」

## 問いかけ

LLM活用にあたり、こんなアイデアは成り立つだろうか、あるいはすでにそんなことは当たり前だろうか、と考えていることがあります。

LLM活用の「インタープリター・モード」と「コンパイル・モード」というアイデアです。

ここに、同じAPIを使って実装されたMCPとCLIがあるとします。

はじめはMCPを使ってものごとの調査を開始し、多くのトークンを使って時間をかけ、タスク群の計画と実行を通じて調査します（インタープリター・モード）。

2回目以降は、同じ手順を「コンパイル」して、高速かつより少量のトークンで実行したい。例えば、2回目以降は同じ手順をCLIを使ったシェルスクリプトに変換して実行する、というものです。

## ChatGPTの回答

はい、十分に成り立つアイデアです。しかも単なる比喩ではなく、かなり有用なシステム設計原理になります。

ただし現在の位置づけは、次のような中間地点です。

- 「エージェントによる探索を、固定ワークフローへ移行する」という考え方はすでに一般的
- 「実行履歴から自動的に再利用可能なCLIプログラムへコンパイルする」仕組みは、まだ当たり前とまではいえない

### 本質は「探索」と「実行」の分離

この提案を整理すると、次のようになります。

| モード | 主な役割 | 特性 |
|---|---|---|
| インタープリター・モード | 未知の問題を調べ、手順を発見する | 柔軟だが、遅くトークン消費が多い |
| コンパイル・モード | 発見済みの手順を再実行する | 高速、安価、再現可能 |
| 再探索モード | 想定外の状況に対応する | 必要な部分だけLLMに戻す |

これは普通のAOTコンパイルより、むしろ次の組み合わせに近いと考えられます。

- JITコンパイル
- プロファイル誘導最適化
- 部分評価
- プログラミング・バイ・デモンストレーション
- エージェント軌跡のワークフロー蒸留

一度の実行だけでなく、実際の実行履歴を観察してから手順を固定化するからです。

### 既存技術との関係

近い研究や実装はいくつかあります。

- **Agent Workflow Memory**は、過去のエージェント実行から繰り返し利用できる手順を抽出し、次の実行を短縮します。([論文](https://arxiv.org/abs/2409.07429))
- **AutoFlow / AFlow**は、エージェントのワークフローを自動生成・最適化し、コードとして表現します。([AutoFlow](https://arxiv.org/abs/2407.12821)、[AFlow](https://arxiv.org/abs/2410.10762))
- **LLMCompiler**は、LLMが逐次判断していたツール呼び出しを実行グラフに変え、並列化してコストと待ち時間を削減します。([論文](https://arxiv.org/abs/2312.04511))
- LangGraphなどでは、あらかじめ経路が決まった「workflow」と、LLMが動的に手順を決める「agent」が明確に区別されています。([公式ドキュメント](https://langchain-ai.github.io/langgraph/agents/tools/))

したがって「方向性そのもの」は既知です。しかし、次のような一貫した製品体験は、まだ十分に標準化されていません。

> MCPを使った探索的実行を記録し、同じAPIのCLI呼び出しへ変換し、次回からLLMなし、または最小限のLLMで実行する。

### MCPとCLIの組み合わせは理にかなっている

理想的には、MCPとCLIを別々に実装するのではなく、共通の中核APIの上に二つのアダプターを載せます。

```text
                   ┌─ MCP adapter ─ LLMによる探索
共通の型付きAPI ──┤
                   └─ CLI adapter ─ 固定ワークフロー実行
```

MCPは、モデルがツールを発見して選択する用途に向いています。公式仕様でも、MCP Toolsは基本的に「model-controlled」とされています。([MCP仕様](https://modelcontextprotocol.io/specification/2025-11-25/server/tools))

一方、CLIは、すでに決まった操作を明示的、再現可能、バッチ処理可能な形で実行するのに向いています。

重要なのは、MCPの会話ログをそのままシェルスクリプトに翻訳するのではなく、途中に中間表現を置くことです。

```yaml
inputs:
  company: string
  period: string

steps:
  - id: fetch_documents
    tool: documents.search
    args:
      query: "${company}"
    retry: 3

  - id: fetch_metrics
    tool: finance.get_metrics
    args:
      company: "${company}"
      period: "${period}"

  - id: summarize
    type: llm
    inputs:
      - fetch_documents
      - fetch_metrics
```

これなら、同じワークフローを次のような実行形式へ変換できます。

- CLIシェルスクリプト
- Pythonプログラム
- CIジョブ
- MCPクライアント
- ワークフローエンジン

### 何をコンパイルできるか

エージェントの実行履歴は、少なくとも三種類に分解する必要があります。

1. **完全に固定化できる処理**

   API取得、フィルター、形式変換、集計、ファイル保存など。CLIや通常のコードにできます。

2. **パラメーター化できる処理**

   前回の「トヨタ」を固定値にせず、`${company}`という入力変数に置き換える処理です。

3. **意味的判断が残る処理**

   「重要なニュースだけ選ぶ」「異常なら追加調査する」などです。ここだけ小さなLLM呼び出しとして残します。

したがって、最適なコンパイル結果は「LLMを完全に消したシェルスクリプト」とは限りません。多くの場合は、次の構成になります。

> 決定論的な骨格＋少数の限定されたLLM判断＋異常時のエージェント復帰

### 難しいのは「一度成功した軌跡」の一般化

単純な記録再生には、次の問題があります。

- 前回たまたま必要だったリトライを恒常的な手順と誤認する
- 検索結果の件数やAPIレスポンス構造が変化する
- 前回の値を固定値にしてしまう
- LLMが行った暗黙の判断条件がスクリプトに残らない
- 書き込み処理を再実行して二重登録する
- APIやCLIのバージョン変更で壊れる
- シェル文字列化によるエスケープやインジェクション問題が起きる

そのため「コンパイル」はコード生成だけでは不十分です。最低限、以下が必要です。

- 入力と固定値の識別
- ツール呼び出しの型情報
- 各ステップの事前条件・事後条件
- 冪等性と副作用の記録
- リトライとタイムアウト
- 複数の入力例による再生テスト
- 想定外の場合にLLMへ戻るガード

### 特に面白いのは「脱最適化」

通常実行はコンパイル済みワークフローで進め、前提が崩れたところだけインタープリター・モードに戻します。

```text
コンパイル済み実行
        │
        ├─ 想定内 ─────────→ 高速に完了
        │
        └─ ガード失敗
               ↓
          LLMが局所的に調査
               ↓
          ワークフローを修正
               ↓
          新しい版として再コンパイル
```

このループまで含めると、単なるスクリプト生成ではなく「自己改善する業務自動化基盤」になります。

### 結論

このアイデアには新規性があります。ただし新規性の中心は「エージェントをワークフローにする」こと自体ではありません。

価値が出るのは、次の一連の体験を統合する部分です。

1. MCPで探索する
2. 実行軌跡を記録する
3. 固定処理・変数・意味的判断を分離する
4. 型付きワークフローに変換する
5. CLIなどで高速実行する
6. 想定外だけエージェントへ戻す
7. 修正結果を再コンパイルする

呼び方としては「インタープリター／コンパイル・モード」で十分通じます。より技術的には、**Agent-to-Workflow Compilation**、**Trajectory Compilation**、あるいは **Agentic JIT** あたりが近い概念です。

特に「MCPを人間・LLM向けの探索インターフェース、CLIをコンパイル先とする」という切り分けは、実装可能性と説明の分かりやすさの両方に優れています。
