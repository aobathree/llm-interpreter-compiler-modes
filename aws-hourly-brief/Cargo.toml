[workspace]
resolver = "2"
members = ["brief-core", "lambda-brief", "lambda-config-api"]

[workspace.dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = "0.4"
ureq = { version = "2", features = ["json"] }

[profile.release]
lto = true
