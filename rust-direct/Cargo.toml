[package]
name = "daily_brief_direct"
version = "0.1.0"
edition = "2021"
description = "bitbank daily brief — direct REST API version (no CLI subprocess)"
license = "MIT"

[dependencies]
serde_json = "1"
chrono = "0.4"
ureq = "2"

[profile.release]
lto = true
