# LLM活用のインタープリター・モードとコンパイル・モード

日付: 2026年8月1日

## 問いかけ

LLM活用にあたり、こんなアイデアは成り立つだろうか、あるいは すでに そんなことは 当たり前だろうか、と考えていることがあります。

LLM活用の インタープリター・モードと コンパイル・モードというアイデア。

ここに 同じ APIを使って実装された MCPと CLIがあるとします。

はじめは MCPを使ってものごとの調査を開始し、多くのトークンを使って 時間をかけて タスク群の計画と実行を通じて調査します。(インタープリター モード)

2回目以降は 同じ手順を「コンパイル」して 高速に より少量のトークンで 実行したい。
例えば 2回目以降は 同じ手順を CLIを使ったシェルスクリプトに変換して 実行するなど。

## 回答(Cursor)

とても良い着眼点です。結論から言うと、**そのアイデアは成り立ちますし、実際にここ1年ほどで業界がまさにその方向へ収束しつつあります**。ただし「完全に当たり前」になったのは一部の形態だけで、ご提案のような「MCP調査 → CLIスクリプトへのコンパイル」という明示的なライフサイクル設計は、まだ体系化の余地が大きい領域です。

### すでに存在する類似の考え方

**1. Code Mode / Code execution with MCP**

Cloudflareの「Code Mode」(2025年9月)や、Anthropicの「Code execution with MCP」(2025年11月)というブログ記事が、ほぼ同じ問題意識を扱っています。MCPツールを1回ずつLLMに呼ばせるとツール定義と中間結果が毎回コンテキストを消費するので、代わりに**LLMにMCPを呼ぶコードを書かせて実行する**方が、トークン量も精度も改善する、という主張です。これはご提案の「インタープリター→コンパイル」の前半にあたります。

**2. Agent Skills / 手順の資産化**

CursorやClaude CodeのSkill(SKILL.md)は、「一度苦労して確立した手順を、次回以降は少ないトークンで再利用可能な形に保存する」仕組みで、これも「コンパイル」の一形態です。ただしSkillは自然言語の手順書なので、実行時に依然としてLLMの解釈(=インタープリター)が挟まります。ご提案のシェルスクリプト化は、そこからさらにLLMを実行パスから排除する点でより踏み込んでいます。

**3. 研究側の先行例**

古くはVoyager(2023年のMinecraftエージェント研究)が「検証済みコードのスキルライブラリを蓄積して再利用する」構成を取っていましたし、DSPyはプロンプトパイプラインに対して文字通り「compile」という語を使っています。概念としての系譜は確立しています。

### ご提案の独自性と価値

「同じAPIを共有するMCPとCLIのペア」を前提に置いている点が実用的で優れています。MCPとCLIが同じAPIの薄いラッパーなら、探索フェーズで確定した呼び出し列を、意味を保ったままCLI呼び出し列に機械的に写像できるからです。効果は明確で、2回目以降は:

- LLMの推論コスト・レイテンシがほぼゼロ(スクリプト実行のみ)
- 再現性が決定的になる(LLMの揺らぎが消える)
- cronやCIに載せられる

### 設計上考えるべき点

実運用でこのパターンをやろうとすると、以下が本質的な課題になります。

- **どこまでコンパイルできるかの見極め**: 手順の中に「結果を見て判断する」ステップが含まれる場合、そこは純粋なシェルスクリプトにできません。判断ロジックが単純なら `if`/`jq` に落とせますが、そうでなければ「決定的な部分はスクリプト、判断点だけ小さなLLM呼び出し」というハイブリッドになります。これはコンパイラ理論でいう部分評価(partial evaluation)に近い構図です。
- **再コンパイルのトリガー**: APIの仕様変更、データ形状の変化などでスクリプトが壊れたとき、**インタープリター・モードにフォールバックして手順を再構築し、再コンパイルする**ループを設計しておく必要があります。JITコンパイラの「deoptimization」に相当します。
- **パラメータ化**: 初回の調査は特定の対象に対して行われるので、コンパイル時に「どこが可変引数で、どこが固定か」をLLMに抽出させる工程が必要です。
- **検証**: コンパイル直後に、インタープリター実行時と同じ入力でスクリプトを走らせて結果が一致するか確認する(ゴールデンテスト)工程を入れると信頼性が上がります。

### まとめ

「LLMは初回の探索と手順発見に使い、確立した手順は決定的なコードに落として再利用する」という方向性は、Code ModeやSkillsの登場が示すとおり業界の潮流そのものです。一方で、**インタープリター/コンパイラという明確な二相モデルとして、フォールバック・再コンパイル・検証まで含めたライフサイクルを設計する**ところまでやっている例はまだ少なく、そこはご自身のツール設計として十分に新規性と実用価値がある領域だと思います。
